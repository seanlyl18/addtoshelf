<!DOCTYPE HTML>
<html>
    <head>
        <title>Your Book Recommendation</title>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='css/styles.css') }}"/>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;1,400&family=Poppins:wght@200;300&display=swap" rel="stylesheet">
    </head>
    <body>
        <div id="container">
            <div class="book-details">
                <img src="{{ bookcover }}" alt="No Book Cover Available"/>
                <div>
                    <h1>{{ title }}</h1>
                    <h2>by <span>{{ author }}</span></h2>
                    <p class="details-1">{{ details }}</p>
                    
                    <div class="divider"></div>

                    {% for tuple in reviews %}
                        {% for review in tuple %}
                            <p class="review-text">"{{ review }}"</p>
                            <div class="line"></div>
                        {% endfor %}
                    {% endfor %}
                </div>
            </div>
            
            <div class="recommend">
                <p>Before you go, why not recommend a book you love to everyone?</p>
                <a href={{ url_for('suggest') }}>Let's Go ➵</a>
            </div>
        </div>
        <div id="footer">
            <p>Shelf | A Project by SeanLYL</p>
        </div>
    </body>
